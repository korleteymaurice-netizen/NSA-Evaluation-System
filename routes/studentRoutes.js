const r=require('express').Router();const c=require('../controllers/studentController');const {requireStudent}=require('../middleware/auth');r.use(requireStudent);r.get('/dashboard',c.dashboard);r.get('/evaluation',c.evaluation);r.post('/book',c.book);r.get('/appointment/:id',c.appointment);r.get('/history',c.history);r.get('/profile',c.profile);r.post('/appointment/:id/cancel',c.cancel);r.post('/appointment/:id/reschedule',c.reschedule);r.get('/api/evaluations/:evaluationId/slots',c.apiSlots);module.exports=r;
